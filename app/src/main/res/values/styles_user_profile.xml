<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Profile" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryWhite</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkWhite</item>
        <item name="colorAccent">@color/colorAccentBlue</item>
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>
    <!-- App Primary Theme -->
    <style name="Profile.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Profile.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />
</resources>